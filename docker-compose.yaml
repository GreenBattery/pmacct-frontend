version: "3.8"

services:
  frontend:
    build: .
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
#    environment:
#      - DB_HOST=host.docker.internal
#      - DB_USER=bandwidth
#      - DB_PASSWORD=secret
#      - DB_NAME=router
